# MDL 发布系统部署手册

## 一、系统架构

```
发布系统容器 (release-backend + release-web)
    │
    ├── SSH (Ansible) ──→ 目标机器 (MDL 服务器，IDC/阿里云各地域)
    ├── HTTP ──────────→ Consul (配置中心)
    ├── HTTP ──────────→ GitLab (配置文件存储)
    ├── HTTP ──────────→ Artifactory (发布包下载)
    └── MySQL ─────────→ 数据库
```

---

## 二、网络要求

### 发布系统容器 → 目标机器

| 协议 | 端口 | 说明 |
|------|------|------|
| SSH  | 22   | Ansible 执行发布/初始化 |

> IDC、阿里云北京/上海/深圳的**所有目标机器**都需要对发布系统容器所在 IP 开放 22 端口。

### 发布系统容器 → 外部依赖

| 目标 | 协议 | 端口 | 说明 |
|------|------|------|------|
| Consul 地址 | HTTP | 80/443 | 推送配置 KV |
| GitLab 地址 | HTTP | 80/443 | 读取配置文件 |
| Artifactory  | HTTP | 80/443 | 下载发布包 |
| MySQL 数据库 | TCP  | 3306   | 持久化存储 |

### 目标机器 → 外部依赖

| 目标 | 协议 | 端口 | 说明 |
|------|------|------|------|
| Consul 地址 | HTTP | 80/443 | consul_pull 拉取配置到本机 |

> 发布包由发布系统本地下载后再上传到目标机器，目标机器不需要访问 Artifactory。

---

## 三、权限要求

### SSH 账号
- 目标机器上需要有统一的 SSH 账号（对应 `docker-compose.yml` 里的 `ANSIBLE_SSH_USER/PASS`）
- 该账号需要有 **sudo 权限**（Ansible 用 `become: true` 执行 systemd、文件写入等操作）
- 建议配置免密 sudo：

```bash
# 在每台目标机器上执行
echo "your_user ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/mdl-deploy
chmod 440 /etc/sudoers.d/mdl-deploy
```

### GitLab Token
- 需要有配置文件所在项目的 **read_repository** 权限
- 填入 `docker-compose.yml` 的 `CONFIG_GITLAB_TOKEN`

### Consul Token
- 需要有以下路径的**读写权限**：`configs/mdl/*`
- 发布系统用于推送配置 KV（write）
- 目标机器 consul_pull 用于拉取配置（read）
- 全局 token 填入 `docker-compose.yml` 的 `CONFIG_CONSUL_TOKEN`
- 各服务实例的 token 在系统页面单独配置

### Artifactory
- 需要能访问 `http://artifactory.datayes.com/artifactory/mdl-release/` 路径下载发布包
- 由发布系统容器访问，目标机器不需要

---

## 四、部署步骤

### 1. 准备数据库

```sql
CREATE DATABASE release_qa CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'alertcenter'@'%' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON release_qa.* TO 'alertcenter'@'%';
FLUSH PRIVILEGES;
```

### 2. 配置 docker-compose.yml

编辑 `docker-compose.yml`，填入实际环境参数：

```yaml
environment:
  - DB_HOST=<数据库IP>
  - DB_PORT=3306
  - DB_NAME=release_qa
  - DB_USER=alertcenter
  - DB_PASSWORD=<密码>
  - CONFIG_GITLAB_URL=<GitLab地址，如 http://git.xxx.com>
  - CONFIG_GITLAB_TOKEN=<GitLab Token>
  - CONFIG_GITLAB_PROJECT_ID=<配置文件所在项目ID>
  - CONFIG_GITLAB_BRANCH=master
  - CONFIG_CONSUL_URL=<Consul地址，如 http://consul.xxx.com>
  - CONFIG_CONSUL_TOKEN=<Consul Token>
  - CONFIG_CONSUL_KV_PREFIX=configs/mdl
  - ANSIBLE_SSH_USER=<SSH用户名>
  - ANSIBLE_SSH_PASS=<SSH密码>
```

### 3. 启动容器

```bash
cd /path/to/release-src
docker-compose up -d
```

### 4. 初始化数据库

```bash
docker exec release-backend python manage.py migrate
docker exec release-backend python manage.py createsuperuser
```

### 5. 配置系统常量

登录后台管理页面 `http://<发布系统IP>:8000/admin`，在 **Constance** 表里配置以下参数：

| Key | 说明 | 示例值 |
|-----|------|--------|
| `ansible_hosts_path` | Ansible hosts 文件路径 | `/datayes/release/ansi/mdl/hosts` |
| `ansible_host_vars_path` | host_vars 文件路径 | `/datayes/release/ansi/mdl/host_vars/release.yml` |
| `ansible_ssh_user` | SSH 用户名 | `your_user` |
| `ansible_ssh_pass` | SSH 密码 | `your_pass` |
| `default_consul_space` | 默认 Consul KV 前缀 URL | `http://consul.xxx.com/v1/kv/configs/mdl/` |

### 6. 访问前端

前端地址：`http://<发布系统IP>:9528`

---

## 五、目标机器初始化

每台新机器首次使用前，在系统页面对该机器执行**初始化**操作，Ansible 会自动完成：

- 安装依赖包（python3、python3-requests 等）
- 创建运行用户和用户组
- 配置 sudoers
- 创建安装目录、备份目录、日志目录
- 生成 systemd service 文件并 enable
- 配置 cron 任务

**前提条件：**
- 目标机器能被发布系统 SSH 连通（22 端口开放）
- SSH 账号有 sudo 权限

---

## 六、服务实例配置说明

在系统页面添加服务实例时需填写以下字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| `install_dir` | 安装目录（含 bin） | `/datayes/forward/bin` |
| `backups_dir` | 备份目录 | `/datayes/forward/backup` |
| `service_name` | systemd 服务名 | `mdl-forward` |
| `executable` | 可执行文件名 | `feeder_handler` 或 `feeder_receiver` |
| `consul_space` | Consul KV 前缀完整 URL | `http://consul.xxx.com/v1/kv/configs/mdl/forward/host01_10.0.0.1/` |
| `consul_token` | 该实例的 Consul Token | `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx` |
| `consul_files` | 需要拉取的配置文件名（逗号分隔） | `feeder_handler.cfg` 或 `feeder_handler.cfg,feeder_receiver.cfg` |

---

## 七、常见问题

| 现象 | 原因 | 解决方案 |
|------|------|----------|
| Ansible SSH 连接超时 | 网络不通或防火墙拦截 | 检查目标机器 22 端口是否对发布系统开放 |
| consul pull 失败 (HTTP 403) | Consul Token 无权限 | 检查 consul_token 是否有该路径读权限 |
| consul pull 超时 | 目标机器无法访问 Consul 地址 | 检查目标机器到 Consul 的网络连通性 |
| start service 超时 | 程序启动异常 | 登录目标机器查看 `journalctl -u <service_name> -n 50` |
| become 权限错误 | SSH 账号没有 sudo | 在目标机器配置 sudoers 免密 |
| 发布包下载失败 | 发布系统无法访问 Artifactory | 检查发布系统容器到 Artifactory 的网络 |
| 备份失败 | 备份目录不存在或无写权限 | 确认已执行初始化，或手动创建 backups_dir |
